// $(document).ready(function() {
//   $("#sort-bookmarks-alphabetically").on("click", function() {
//     // $("#bookmark-sort").text('<% @list.bookmarks_by_name.each do |bookmark| %>')
//   })
//   $("#sort-bookmarks-by-age").on("click", function() {
//     // $("#bookmark-sort").text('<% @list.bookmarks_by_age.each do |bookmark| %>')
//   })
//   $("#sort-bookmarks-by-url-length").on("click", function() {
//     var stuffToSort = $(".display > a")
//     stuffToSort.sort(function(a,b) {
//       return $(a).attr("href").length > $(b).attr("href").length;
//     })

//     stuffToSort.detach().appendTo($("body"));
//     // $("#bookmark-sort").text('<% @list.bookmarks_by_url_length.each do |bookmark| %>')
//   })
// })